# Purpose
A weather Dashbaord application that allows users to search for the current weather and forecast of a city using the OpenWeatherMap API. The application will have a frontend built with JavaScript and a backend built with Flask (Python). It will also include caching to improve performance and reduce API calls.

## NEVER_MODIFY CODE
- style.css